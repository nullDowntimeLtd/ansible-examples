---
- hosts: riak_cluster
  serial: 1
  user: root
  remote_user: vagrant
  sudo: true
  ask_sudo_pass: false
  pre_tasks:
  - name: make sure there are no transfers happening
    riak: wait_for_handoffs=600
  roles:
  - riak/common
