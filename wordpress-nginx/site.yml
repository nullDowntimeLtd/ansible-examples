---
- name: Install Wordpress, MySQL, Nginx, and PHP-FPM
  hosts: wordpress-server
  user: root
  remote_user: vagrant
  sudo: true
  ask_sudo_pass: false

  roles:
    - common
    - mysql 
    - nginx
    - php-fpm
    - wordpress
